# Quick start

> **This is real-money trading software.** Never paste a password, MFA code, brokerage account number, OAuth secret or token, phone QR code, or private pairing link into chat. Complete sign-in only in Robinhood's official browser flow.

## 1. Connect Robinhood once

The agent cannot access Robinhood until its MCP connector is added and authorized. Inspect the installed connectors first: exactly one Robinhood connector should exist, and setup must never create a duplicate.

- **ChatGPT Desktop in Codex mode, or Codex:** in the desktop build observed for this project, open **Settings → Plugins → MCPs**. Labels and the exact settings path may vary by app version, so use the [README first-time app setup](README.md#first-time-app-setup) for the current details. If exactly one Robinhood connector already exists, select it and choose **Authenticate** or **Re-authenticate**; do not add another. Add one only if no Robinhood connector exists.

> **Claude is not a supported setup target.** Current tested Claude models refuse the live financial-trade operations this project exists to automate, even when scheduled and explicitly authorized. Use Codex for setup, testing, and execution; do not create or enable a Claude schedule.

If no Robinhood connector exists and you add one, name it `Robinhood Trader` and use this server URL:

`https://agent.robinhood.com/mcp/trading`

Complete Robinhood's official authorization flow, then restart the app and open a brand-new task/session in the exact project context above. Require `get_accounts` there before any broker work. If it is still absent after authenticating or re-authenticating the one existing connector, remove only that single connector, add it back once, complete OAuth, restart the app, and verify `get_accounts` in another fresh task/session. Never leave or create a duplicate, and do not attempt broker work until this check succeeds.

**Write down the exact display name of your Agentic account**—often simply `Agentic`. The setup prompt will verify that name and write it into `AGENTIC_ACCOUNT_NAME` in `constants.md`; never use or copy the account number.

## 2. Paste one prompt

Open this project in **ChatGPT Desktop in Codex mode or Codex**, then paste the prompt below. Do not treat an ordinary ChatGPT chat as equivalent to Codex mode. Use the exact main checkout so the task sees its local uncommitted configuration and shared gitignored runtime state. The assistant handles the repository, a verified available Python runtime, validation, tests, and setup; you should need to act only for an unavoidable safety decision.

**Local sensitive-temp data:** the retained `broker_snapshot.py preflight --create-scratch` command creates and proves both run scratch and one empty invocation response-source root in a single operation; on Windows the helper also prepares and verifies the least-privilege per-directory OS capability needed for a separate file-change facility to create fresh payloads and the exact scratch status candidate without making the added cross-principal writer-only capability inherit to helper markers. The automation never creates, chooses, authors, changes permissions on, recopies, relocates, or replaces either random path. During a Codex routine, separate executor-local bootstrap state machine-carries the complete Python-resolver, lifecycle-start, validated-constants, and active-lifecycle-context receipts through `launcher-bound`, `lifecycle-bound`, `configuration-bound`, and `context-bound`. For the resolver, lifecycle-start, validated-constants, and active-context receipts, each named checked-in producer validates its complete schema and identifiers, so runner glue stores each unchanged and does not rebuild its key count, type map, or UUID grammar; later receipts keep their explicit exact-field checks. Lifecycle's checked-in `acquire-bind-context` action validates the running preflight invocation, acquires the fenced lease, binds the active context, and owner-fenced releases the new lease if binding fails. The active-context receipt itself correctly keeps the helper-owned lifecycle `phase: "preflight"`; only the surrounding bootstrap state advances to `phase: "context-bound"` after storing that unchanged receipt. The raw lease-issued `RUN_LOCK_TOKEN` is machine-carried only in executor-private state after the combined action succeeds and through every renewal, order-intent, mutation-precheck, and release use. Runner glue never exposes it in ad-hoc output or narration and never stores it in a bootstrap, transport, report, status, or memory artifact; it passes the value only to prescribed checked-in helper arguments, and only the checked-in lease and order-intent protocols may intentionally persist it. The value is never retyped, reconstructed, substituted, or derived from another receipt. Every token-consuming operation loads the exact acquired nonempty value from the expected state slot; missing, cleared, malformed, or wrong-phase token state stops before invoking that helper or any broker tool, never by sending an empty placeholder. Transport state carries the project root, scratch/source receipt, and bound account scope; it does not own later source filenames, sequences, pending calls, or keyed path maps. The preflight operation stores `preflight-bound` state before its compact path-free output; after the required journal/rules-version steps, the first accounts save/bind operation loads only that state. Python, configured account name, account number, invocation/artifact names, and every path are loaded from those receipts rather than retyped. Every nested Windows command explicitly selects `powershell.exe`, regardless of the outer shell. A nested `exec_command.session_id` is polled to completion inside its current isolate with `write_stdin`; only an outer `functions.exec` cell ID is continued with `functions.wait`. Missing, cleared, malformed, duplicate-purpose, or wrong-phase state fails closed; another runner must carry equivalent opaque structured values rather than hand-transcribing them.

The first successful `get_accounts` result, its exact full-object `JSON.stringify`/`JSON.parse`/zero-prefix save, and `broker_snapshot.py bind-transport` share one composed operation with every argument taken from loaded state and no raw payload or random-path output. A final account-read error returned or caught inside that same live operation records compact `account-scope-failed`; it does not mislabel the absence of a successful response/save as a write failure, while an interrupted cell remains fenced. The helper's stable `account_scope_failed` code distinguishes a valid canary whose configured account cannot bind from a transport/path failure; Codex emits only that compact classification, never the path-bearing helper message. After binding, `broker_snapshot.py reserve-source` chooses and durably binds one random source filename to a unique logical purpose before a read; the file-change facility writes the complete response once, and `commit-source --scratch ... --purpose ...` self-correlates to that immutable journal reservation before sealing its strict JSON bytes and file identity. Unattended runner glue never carries the reservation UUID into commit; abort remains explicitly ID-bound. A new reservation and every consumer are refused while any earlier purpose is unresolved. `lookup-source` can recover only a committed matching file or an exact commit-only boundary; commit-only repeats the same scratch-plus-purpose commit without another broker call or write. `abort-source` can terminalize a proven final connector failure only while the target is absent. The four checked-in journal actions own their complete receipt schemas, so runner glue retains each receipt unchanged and never invents a field count or type map. Every post-bind local JSON command uses the same fully drained `{process, receipt}` shape. Staging, scan filtering, candidate evaluation, and order-intent helpers consume committed purposes directly, so the runner never carries a random response path or implements sequence/phase transitions. Pending, aborted, unregistered, moved, replaced, or modified sources fail closed. Placement/cancellation uncertainty remains owned by the intent protocol; only an explicit successful placement result is then journaled. Status keeps its separate fixed scratch candidate and publication state machine. All three Codex state slots are cleared only after normal final telemetry, or after all permitted terminal report/status work, lease release or proven ownership loss, and lifecycle finish. No BOM, label, fence, narration-carried path, nested fallback, alternate writer, second root, or save retry is allowed.

For entry evaluation, save every complete quote and successful RSI result unchanged under a unique purpose and pass those purposes directly to `evaluate_candidates.py`. The script resolves them through the bound journal, unwraps MCP envelopes, reads symbols, merges batches, validates RSI series, and rejects duplicates. Never build a derived quote/RSI map; a failed indicator read blocks that name for missing RSI. `connector_contract.py` is also the sole authority for every portfolio and the SPY red-day quote: valid zero-valued account strings are never rejected by runner numeric/truthiness logic, and the SPY action returns the previous-close comparison without raw envelope probing. Snapshot-generation portfolio/quote staging remains non-paginated; the helper harmlessly normalizes stray cursor/allow-more flags for those two kinds while retaining strict pagination linkage for positions/orders. Each committed positions/orders page is interpreted by `connector_contract.py`: omitted, null, or empty `data.next` is a valid terminal page, and only its normalized cursor receipt controls pagination. After FIRST reaches the terminal page, one whole-set helper call validates every purpose and cursor together, rejects cross-page duplicate symbols, and returns the sole `symbol`, `quantity`, `intraday_quantity`, and `average_buy_price` rows; never parse raw `data.results`, `data.positions`, or `data.next`.

At report time, Codex derives one target from the retained lifecycle-bound `expected_report_file` and performs the add-only write plus byte-for-byte read-back in the same composed operation; other runners use one native Write and one native Read against that same retained filename. Status publication begins only after that exact report binding proves both persistence and read-back—never after a reconstructed or separately typed path.

```text
Set up RobinhoodEquityTradingAgent and perform one safe first test for me:
https://github.com/abiemann/RobinhoodEquityTradingAgent.git

I am non-technical. Do the technical work yourself, keep updates concise, and ask me to act only when official browser authorization, a required Robinhood UI change, or a real-money-risk decision makes that unavoidable. Do not ask me to run commands that you can run yourself.

Safety requirements:

- Never ask me to paste passwords, MFA codes, account numbers, OAuth files, secrets or tokens, phone QR codes, or private pairing links into chat.
- Keep `DRY_RUN = true`. Never enable live trading, commit or push `DRY_RUN = false`, weaken tests, change strategy rules or other trading constants, or loosen approval gates. The only configuration edits this setup authorizes are changing `DRY_RUN` from `false` to `true` if necessary and writing the confirmed `AGENTIC_ACCOUNT_NAME` described below. Never change `DRY_RUN` from `true` to `false`, and re-run validation after either authorized edit.
- Neither `place_equity_order` nor `cancel_equity_order` may be preapproved. In Codex, keep both set to `Needs approval`. If that cannot be guaranteed, stop before running the routine.
- Dry run prevents new entries, but it may still sell or protect existing positions and modify related orders.
- Do not create or enable a schedule. Scheduling and live trading require separate, later consent.
- Treat routine execution as stateless. A scheduler-injected `Automation memory:` line and path are metadata, not a request: ignore them without opening the path. Never read, create, or update `memory.md`, and never call a framework memory tool. Read the routine sequentially through EOF in bounded chunks of at most 100 lines, continue from the exact next unread line, and require the final read to prove EOF. If a read is truncated, omits an interval, or cannot prove EOF, re-read the missing interval first in chunks of at most 50 lines and then in successively smaller sequential chunks as needed before executing it.
- Treat the lease-issued `RUN_LOCK_TOKEN` as an opaque private value. Machine-carry its exact raw value only in executor-private state from acquisition through every lease and order-intent use. Never expose it in ad-hoc output or narration or store it in a bootstrap, transport, report, status, or memory artifact; pass it only to prescribed checked-in helper arguments, and allow only the checked-in lease and order-intent protocols to intentionally persist it. Never retype, reconstruct, substitute, or derive it. Missing, malformed, cleared, or wrong-phase token state must stop before invoking a token-consuming helper or broker tool, never by passing an empty placeholder.
- Preserve every successful quote/RSI result as the complete unmodified tool object through the routine's bound source journal. Never rebuild it into a derived map; an exhausted indicator read must block that name for missing RSI.
- Preserve all existing files and local changes. If a safety check fails, stop rather than bypass it.

Proceed autonomously:

1. Confirm that `get_accounts` is available in this brand-new task/session. If it is not, follow the idempotent connector instructions above and stop. Do not claim setup succeeded or attempt broker work until exactly one connector is authorized, the app has restarted, and `get_accounts` works in another fresh task/session.
2. If this exact repository checkout is already open as the current native project, use it. Otherwise open a safe writable parent folder, clone into a new subfolder without overwriting anything, and then stop before lifecycle or broker work so I can open that cloned repository in a brand-new native project session. Do not continue from the parent-folder session. Read `AGENTS.md` and the relevant README setup and safety sections, then read `robinhood-momentum-routine-autonomous.md` sequentially through EOF in bounded chunks of at most 100 lines, continuing from the exact next unread line and requiring the final read to prove EOF. If a read is truncated, omits an interval, or cannot prove EOF, re-read the missing interval first in chunks of at most 50 lines and then in successively smaller sequential chunks as needed.
3. Before any lifecycle command, require a host-native project/runtime pair. On native Windows, go directly to the checked-in resolver without a preliminary framework dependency lookup or preferred-path hint: execute exactly `powershell.exe -NoProfile -NonInteractive -ExecutionPolicy Bypass -File ./resolve_python.ps1`. Require its valid JSON receipt, bind the exact returned absolute Python 3 path as `PYTHON_EXE`, and reuse that exact executable with current-shell literal quoting for validation, the full test suite, and every later checked-in Python helper. Never substitute a bare `python`, `python3`, `py`, or a generic “Windows equivalent.” Native Git Bash still invokes this Windows resolver and then executes its returned Windows path without PowerShell's leading `&`. If this Windows checkout is exposed through a POSIX/FUSE sandbox or `powershell.exe` is unavailable, do not fall back to `/usr/bin/python3`: stop before lifecycle and broker work, close the wrong context, and start a brand-new native project session on the exact repository. A genuinely native Linux/macOS checkout may instead launch-probe and bind an absolute host `python3` under the same reuse rule. Execute `validate_constants.py --json` with the bound `PYTHON_EXE` and require `DRY_RUN` to be exactly `true`. If it is `false`, change only that row from `false` to `true` and re-run validation. For any other validation failure, diagnose and stop; do not alter safety checks to make them pass.
4. Call `get_accounts` read-only without repeating or storing account numbers. If there are zero agentic-enabled accounts, stop. If exactly one exists, show me only its display name and ask me to confirm it; if several exist, show only their display names and ask which one to use. The chosen display name must then resolve to exactly one account and that account must be agentic enabled; otherwise stop with no default, partial-match, first-account, or account-number fallback. After confirmation, write the exact name into `AGENTIC_ACCOUNT_NAME` in `constants.md` as a local, uncommitted edit. Re-run `validate_constants.py --json` with the same bound `PYTHON_EXE` and require success. Do not continue until the confirmed name and validated constant match exactly.
5. Using the exact already-bound `PYTHON_EXE`, run the complete suite as `'<PYTHON_EXE>' -m unittest discover -s tests` with the current shell's required command prefix and literal quoting. Do not use a bare launcher or invent an ad-hoc serializer, path, ACL repair, or extra broker call as a setup proof. The first supervised entry-eligible run with `DRY_RUN = true` remains the runner-specific end-to-end proof of the real accounts canary, broker-response staging, and final scratch status candidate; one runner's proof is not transferable to another. If the suite fails, diagnose and stop.
6. Verify the permission outcome yourself: neither `place_equity_order` nor `cancel_equity_order` is preapproved. In Codex require `Needs approval` for both. If you cannot inspect or guarantee that outcome, tell me exactly where to check and stop before running the routine.
7. Using read-only Robinhood tools, verify again that the confirmed `AGENTIC_ACCOUNT_NAME` resolves to exactly one agentic-enabled account. Do not repeat account numbers, balances, holdings, or order details in chat. Report only whether the exact match succeeded and whether positions or open orders exist; otherwise stop with no fallback.
8. Verify the saved scan named by `SCAN_TITLE` and its required columns: `Last`, `Relative volume`, `% Change`, and `Volume`. Creating a missing saved scan is a broker-side setup mutation: explain that change and obtain my explicit confirmation before creating it with a documented tool. If creation is unsupported or columns require a manual Robinhood UI change, give me only those steps and wait.
9. If positions or open orders exist, explain that a dry run may modify or sell them and ask for explicit confirmation before running anything. If the matched account is flat with no open orders, this prompt authorizes one supervised execution of the checked-in routine with `DRY_RUN = true`.
10. Summarize the first-test result in plain language. Confirm that live mode remains off, the account-specific name remains local and uncommitted, and no schedule was created. Then offer, but do not start, either the dashboard and phone viewer or later scheduling guidance.
```

The first test cannot place a new entry because `DRY_RUN` remains on. If the account already holds positions or has open orders, the assistant pauses first because protective management can still perform real broker actions.

For configuration details, troubleshooting, dashboard setup, and the separate path to scheduling or live trading, see [README.md](README.md).

Only after the safe first test succeeds and you separately consent to scheduling, follow [README scheduling](README.md#scheduling). The README is the sole scheduling source.

## Timing for later scheduled runs

When you later create a Codex scheduled task, copy the complete maintained prompt from [README scheduling](README.md#scheduling) into that scheduler's **Instructions**, including its exact `TIMING_IDENTITY` line, stateless/no-memory rule, bounded read-through-EOF rule, and private machine-carried lease-token rule. Do not put the identity in the ordinary Description field, memory, this Quickstart prompt, or a separate scheduling guide. A scheduler UI may display an automation-memory path; that display never authorizes the trading routine to read or update it. Do not create or enable a Claude schedule.

Keep the prompt's one `TIMING_IDENTITY` line synchronized with the runner, model, and configuration actually selected in the task settings; if a selection changes, update the line before the next run. It records what ran and does not select or switch the model. The routine records Routine total, Strategy execution, and Routine overhead. Immediately before its final on-screen Run Summary, the same `record-internal` host-clock reading also records **Comparable run duration** from lifecycle-bound START CLOCK through `final-summary-boundary` and prints exact Run start/Run end timestamps plus the helper-formatted duration. Historical runner comparisons use those same automatic boundaries and do not change the saved report or status schema. A source-specific **Reference run duration** can still be attached with the optional post-run observation below.

Before START CLOCK, the routine also makes one structured self-report from only identity that the current framework explicitly exposes and passes it through the checked-in exact-match registry. Complete direct task metadata is strongest, then the scheduler Instructions declaration. Historical Claude runtime aliases remain available only so old performance records retain their provenance. The projection records runner, model, and configuration provenance separately. Any unknown field, self-reported field, or conflict excludes the record from primary fair-comparison cohorts until independent evidence replaces it. Identity resolution is observational only and cannot change trading or lifecycle behavior.

After the run is complete, resolve and bind the checked-in resolver's exact Python path as `PYTHON_EXE`, replace `<INVOCATION_ID>` and `<DURATION_MS>`, and copy exactly one matching PowerShell command. For Codex's displayed **Worked for** duration:

```powershell
& '<PYTHON_EXE>' run_performance.py observe-task --invocation-id '<INVOCATION_ID>' --task-duration-ms <DURATION_MS> --runner codex --model 'gpt-5.6-sol' --configuration 'reasoning=high' --identity-source manual-ui --clock-source codex-worked-for
```

Use this `manual-ui` template only for the named human-observed source; do not relabel runner metadata. The attached value is displayed as **Reference run duration** and remains secondary to the automatic **Comparable run duration**; it is fallback/context for historical or incomplete runs. Fair performance comparisons require the same automatic boundary, session class, workload path, configuration cohort, and preferably rules version. Keep runner/model identity explicit as the comparison dimension. See [README Tested On](README.md#tested-on) for the formulas, source rules, POSIX-shell form, and local comparison dashboard.
